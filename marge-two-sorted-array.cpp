// akash change
#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> arr1{2,10,11,14};
    vector<int> arr2{3,4,5};
    int gap = (arr1.size()+arr2.size())/2;
    if(gap%2) gap++;
    while (gap>=1)
    {
        // int i = 
    }
    
    return 0;
}